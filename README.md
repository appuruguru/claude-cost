# claude-cost — Claude Code marketplace

A single-plugin marketplace. The plugin lives in `plugins/claude-cost/`.

## Quick start
```
/plugin marketplace add appuruguru/claude-cost
/plugin install claude-cost@claude-cost
/reload-plugins
/claude-cost:setup
```
See `plugins/claude-cost/README.md` for full docs.
